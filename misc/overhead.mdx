---
title: "Rank Overhead System"
description: "The follow commands are to control the rank over your avatars head."
---

- Use `/hidegui` to hide the rank that is above your head,
- Use `/showgui` to show the rank that is above your head.

_There is no way to change the color, name, or hide the tag inside the chat_